1. Доступность и корректность работы:
✅ Сайт открывается, основные страницы доступны.

❌ Битые ссылки: Некоторые ссылки ведут на несуществующие страницы (например, разделы "Услуги", "Контакты").

❌ Навигация: Нет явной ссылки на домашнюю страницу в некоторых разделах.

❌ Формы:

Нет подсказок для полей ввода.

Обязательные поля не помечены символом *.

Нет сообщений об успешной отправке данных.

2. Вёрстка и адаптивность:
✅ Кодировка UTF-8 используется, шрифты загружаются.

❌ Адаптивность:

На мобильных устройствах элементы накладываются друг на друга.

Скроллбар появляется некорректно при изменении размера окна.

❌ JavaScript: В консоли есть ошибки (например, Uncaught TypeError).

3. Дизайн и юзабилити:
❌ Кнопки: Не все кнопки в едином стиле (разный размер, цвет).

❌ Текст:

Есть орфографические ошибки (например, "преветствие" вместо "приветствие").

Выравнивание текста не всегда единообразное.

❌ Формы:

Выпадающие списки иногда обрезаются.

Не все поля доступны с клавиатуры (проблемы с Tab-навигацией).

4. Технические аспекты:
✅ Favicon присутствует.

❌ Cookies: При удалении cookies сайт не сохраняет сессию, но не выдаёт уведомлений.

❌ Загрузка документов: Некоторые PDF-файлы не открываются.
